# The main pipeline for github_dabs
resources:
  pipelines:
    github_dabs_pipeline:
      name: github_dabs_pipeline
      catalog: dacascan_ws1
      schema: github_dabs_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
