# This is a Databricks asset bundle definition for github_dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: github_dabs
  uuid: c98c02b2-5f5a-42bd-ad3e-de3e55a8d175

# artifacts:
#   python_artifact:
#     type: whl
#     build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # Development environment - resources get prefixed with [dev]
    mode: development
    default: true
    workspace:
      host: https://adb-520209755093735.15.azuredatabricks.net
      # Development files go to a dev-specific path
      root_path: /Workspace/Users/david.cascante@databricks.com/.bundle/${bundle.name}/dev

  prod:
    # Production environment - full production mode
    mode: production
    workspace:
      host: https://adb-520209755093735.15.azuredatabricks.net
      # Production files go to a prod-specific path
      root_path: /Workspace/Users/david.cascante@databricks.com/.bundle/${bundle.name}/prod
    permissions:
      - user_name: david.cascante@databricks.com
        level: CAN_MANAGE
